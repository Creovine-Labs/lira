# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
playwright-report
test-results
*.png
!public/lira_logo.png
!public/favicon.png
!public/apple-touch-icon.png
!public/landing-v2/
!public/landing-v2/*.png
.env
.env.local
.env.*.local
.vercel
public/participants/*.mov
docs/microsoft_secrets.txt
docs/salesforce_secrets.txt
docs/replytoken.txt
*.bak
docs/aws_secrets.txt
docs/LIRA_MARKET_RESEARCH.md

# Locally-built widget bundle for dev-mode testing — Vite serves /public/*
# in dev; we copy `npm run build:widget`'s output here so the demo page can
# load the unreleased widget without pushing to the CDN. Not for commit.
public/dev/


# Confidential — client docs and internal strategy; repo is public, never commit
Riverly_docs/
docs/lira-product-vision-v1.txt
docs/platform/customer-support/RIVERLY_PILOT_PLAN.md
docs/cartesia-api.txt

# All internal docs — repo is public, keep the entire docs tree out
docs/

# Vercel / production env files — contain secrets, never commit
.env.vercel.production
.env.vercel*
.env.production
.env.production.local

# Local project/infra notes — public repo, keep out
CLAUDE.md
